This counts how many? symbols are present in that column.
Remove the column or rows if missing values are very high. Then change the datatype to whole number or decimal number.
if you want to calculate age in excel using the date of birth and the dataset collection date 8 june 2023 used DATEDIF formula.
To convert the charges column into currency.
Using Vlookup formula Combine all three table into one master sheet name "Healthcare".
Finally using the Pivot table calculate average details .
Overall, the new create dashboard sheet and added KPI deatails.
